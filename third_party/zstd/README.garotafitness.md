[zstd](https://github.com/facebook/zstd), tag `v1.5.5` (BSD-3-Clause / GPLv2, `LICENSE`).

Decompress sources are unmodified. `stream/xt3u` wraps `ZSTD_DCtx_refPrefix` for XTool patched streams (`PrecompUtils.pas` `PrecompDecodePatchEx`).
